import './Navbar.css';

export const Navbar = () => {
  return (
    <div className="nav-container">
      <a href="#home">Home</a>
      <a href="#about">About</a>
      <a href="#menu">Menu</a>
      <a href="#contact">Contact</a>
    </div>
  );
};

export default Navbar;
